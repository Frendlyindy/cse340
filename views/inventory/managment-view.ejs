<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>

    <% if (message) { %>
      <p class="notice"><%= message %></p>
      <% } %>
      

<div>
    <a href="/inv/new-class">Add new classification</a>
    <a href="/inv/new-car">Add new vehicle</a>
</div>